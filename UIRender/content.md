# Sudhir Pathak — UI Renders

> Interactive UIs and web apps — each one in its own folder.

Browse the UIs below. This section is **directory-based**: create or drop a **folder** (containing an `index.html`) into `UIRender/` and it appears here automatically.

## Notes

Add a short note for each folder so it's easy to refer back later.
Format (one per line):  `- folder-name — your short note`
Folders without a note still show up; they just won't have a description.

- sample-ui — Starter template showing the folder structure. Duplicate it to build your own UI.
